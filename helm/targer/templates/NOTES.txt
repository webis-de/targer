Thank you for installing {{ $.Chart.Name }} (as release name {{ $.Release.Name }}){{ with $.Values.namespace }} in the {{ . }} namespace{{ end }}.

The {{ $.Chart.Name | title }} backend is proxied at https://{{ $.Values.backend.host }}{{ $.Values.backend.path }}.
It runs on internal service port {{ $.Values.backend.servicePort }} with {{ $.Values.backend.replicas }} replicas.
Models are mounted at {{ $.Values.backend.modelsPath }}.
This path MUST be readable from each node and contain all models.

The {{ $.Chart.Name | title }} frontend is proxied at https://{{ $.Values.frontend.host }}{{ $.Values.frontend.path }}.
It runs on internal service port {{ $.Values.frontend.servicePort }} with {{ $.Values.frontend.replicas }} replicas.

You have configured {{ $.Chart.Name | title }} to access the Elasticsearch index
{{ $.Values.elasticsearch.index }} on {{ $.Values.elasticsearch.host }}:{{ $.Values.elasticsearch.port }}.
{{ if and $.Values.elasticsearch.username $.Values.elasticsearch.password }}
Elasticsearch authentication is configured for user {{ $.Values.elasticsearch.username }}.
{{ else }}
Elasticsearch authentication is not configured. Configure credentials with the `--set elasticsearch.username=EXAMPLE --set elasticsearch.password=EXAMPLE` options for the `helm` command, if you need to access a restricted Elasticsearch cluster.
{{ end }}
