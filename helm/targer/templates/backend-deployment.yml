apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Chart.Name }}-backend
  name: {{ .Chart.Name }}-backend
spec:
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-backend
  replicas: {{ .Values.backend.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-backend
    spec:
      containers:
      - name: {{ .Chart.Name }}-backend
        image: {{ .Values.backend.image }}
        volumeMounts:
        - mountPath: /emnlp2017-bilstm-cnn-crf/models/
          name: {{ .Chart.Name }}-models
          readOnly: true
      restartPolicy: Always
      volumes:
      - name: {{ .Chart.Name }}-models
        hostPath:
          path: {{ .Values.modelsPath }}
          type: Directory
