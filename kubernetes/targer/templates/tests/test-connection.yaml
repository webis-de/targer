apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "targer.fullname" . }}-test-connection"
  labels:
    {{- include "targer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "targer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
