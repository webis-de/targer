apiVersion: v1
kind: Service
metadata:
  name: {{ include "targer.fullname" . }}
  labels:
    {{- include "targer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "targer.selectorLabels" . | nindent 4 }}
